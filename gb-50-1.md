<!-- 改编自：博客主题项目提示词/gb-50-1/gb-50-1.md；后端由 NestJS + TypeScript + TypeORM 改造为 Go 1.22 + Gin + GORM -->
# gb-50-1 功能完善的个人博客系统

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 1. 项目概述

**功能完善的个人博客系统（gbblog）** 是一个面向个人博主的内容发布与阅读平台，支持文章发布、分类管理、标签体系、评论互动、全文搜索和归档浏览，博主可登录后台管理文章、分类、标签与评论审核。项目类型：**全栈 Web 应用**。

### 2. 核心实体（≥ 3 个，必须贯穿全栈）

- **User（用户）**
  - 字段：id, username, email, password_hash, nickname, avatar, bio, social_links（JSON）, role（admin/visitor）, created_at, updated_at
  - 关键关系：博主可创建多个 Article；游客可发表多个 Comment
  - 业务动作：注册/登录、修改资料与密码、上传头像
  - **此实体必须贯穿：数据库表 → backend/internal/model/user.go → backend/internal/repository/user_repository.go → backend/internal/service/user_service.go → backend/internal/handler/user_handler.go → backend/internal/router/users.go → frontend/src/api/user.ts → frontend/src/stores/userStore.ts → frontend/src/pages/Admin.tsx**

- **Category（分类）**
  - 字段：id, parent_id, name, slug, description, sort_order, created_at
  - 关键关系：多级分类自关联（parent_id），拥有多个 Article
  - 业务动作：创建/修改/删除分类、按分类筛选文章、构建分类树
  - **此实体必须贯穿全栈，禁止在单一文件中处理 CRUD**

- **Tag（标签）**
  - 字段：id, name, slug, created_at
  - 关键关系：与 Article 多对多关联（article_tags）
  - 业务动作：创建/修改/删除标签、按标签筛选文章
  - **此实体必须贯穿全栈**

- **Article（文章）**
  - 字段：id, user_id, category_id, title, slug, summary, content_markdown, content_html, cover, status（draft/published/scheduled）, is_top, published_at, view_count, word_count, created_at, updated_at
  - 关键关系：隶属于 Category 与 User，拥有多个 Comment 和 Tag
  - 业务动作：Markdown 撰写、草稿保存、定时发布、置顶、全文搜索、归档统计
  - **此实体必须贯穿全栈**

- **Comment（评论）**
  - 字段：id, article_id, parent_id, user_id, nickname, email, content, status（pending/approved/deleted）, created_at
  - 关键关系：隶属于 Article，支持最多三层嵌套回复
  - 业务动作：游客/登录用户评论、嵌套回复、评论审核
  - **此实体必须贯穿全栈**

- **ArticleTag（文章-标签关联）**
  - 字段：article_id, tag_id, created_at
  - 关键关系：连接 Article 与 Tag 的多对多关系表
  - 业务动作：文章打标签、按标签聚合文章
  - **此实体必须贯穿全栈**

### 3. 核心页面（≥ 4 个，至少 2 个页面共用同一业务组件）

- **/（首页）**
  - 消费实体：Article、Category、Tag
  - 关键交互：最新文章列表、置顶文章、站点统计（文章总数/分类数/标签数/总字数）、搜索入口
  - 共享组件：`<ArticleCard>`、`<StatsCards>`

- **/articles/:slug（文章详情）**
  - 消费实体：Article、Comment、User、Tag
  - 关键交互：Markdown/HTML 渲染、关键词高亮、嵌套评论、评论提交
  - 共享组件：`<CommentList>`、`<ArticleStatusBadge>`

- **/categories/:slug（分类文章列表）**
  - 消费实体：Category、Article
  - 关键交互：分类树导航、分类下文章分页
  - 共享组件：`<ArticleCard>`（与 / 共用）、`<CategoryTree>`

- **/tags/:slug（标签文章列表）**
  - 消费实体：Tag、Article
  - 关键交互：标签云、标签下文章分页
  - 共享组件：`<ArticleCard>`（与 / 共用）

- **/archives（文章归档）**
  - 消费实体：Article
  - 关键交互：按年月时间线分组归档、点击跳转文章
  - 共享组件：`<TimelineList>`

- **/search（搜索结果）**
  - 消费实体：Article、Tag
  - 关键交互：全文搜索、关键词高亮、相关度排序
  - 共享组件：`<ArticleCard>`

- **/admin（后台管理）**
  - 消费实体：Article、Category、Tag、Comment
  - 关键交互：文章 CRUD、Markdown 编辑器、分类/标签管理、评论审核
  - 共享组件：`<ArticleStatusBadge>`（与 /articles/:slug 共用）、`<CategoryTree>`、`<MarkdownEditor>`

### 4. 横切关注点（≥ 2 个）

- **认证授权（JWT + RBAC）**
  - 触达文件层：
    - 数据库：`users` 表含 role 字段
    - 后端：`internal/middleware/auth.go`、`internal/middleware/rbac.go`、`internal/util/jwt.go`
    - 路由：`backend/internal/router/*.go` 中后台管理接口需 `AuthRequired()` 和 `RequireRole(admin)`
    - 前端：`router/guards.ts`、`components/common/RoleGuard.tsx`、`stores/authStore.ts`

- **接口限流**
  - 触达文件层：
    - 后端：`internal/middleware/rate_limiter.go`
    - 路由：登录、评论提交等接口启用限流
    - 配置：`internal/config/config.go`

- **全局错误处理**
  - 触达文件层：
    - 后端：`internal/middleware/error_handler.go`、`internal/util/app_error.go`、`internal/constants/error_codes.go`
    - 前端：`utils/request.ts` 拦截器

### 5. 共享枚举/常量（≥ 2 组）

- **ArticleStatus（文章状态）**
  - 值：DRAFT = 'draft', PUBLISHED = 'published', SCHEDULED = 'scheduled'
  - 前端定义位置：`frontend/src/constants/article.ts`
  - 后端定义位置：`backend/internal/constants/article.go`
  - 出现位置：Article 模型、ArticleService、ArticleStatusBadge、文章列表、定时发布任务、日志模板、错误码
  - **README 必须列出该枚举在前后端的所有出现位置**

- **CommentStatus（评论状态）**
  - 值：PENDING = 'pending', APPROVED = 'approved', DELETED = 'deleted'
  - 前端定义位置：`frontend/src/constants/comment.ts`
  - 后端定义位置：`backend/internal/constants/comment.go`
  - 出现位置：Comment 模型、CommentService、评论审核、CommentList、日志模板、错误码
  - **README 必须列出该枚举在前后端的所有出现位置**

- **UserRole（用户角色）**
  - 值：ADMIN = 'admin', VISITOR = 'visitor'
  - 前端定义位置：`frontend/src/constants/user.ts`
  - 后端定义位置：`backend/internal/constants/user.go`
  - 出现位置：User 模型、RBAC 中间件、路由权限、前端按钮显隐、路由守卫
  - **README 必须列出该枚举在前后端的所有出现位置**

### 6. 共享前端组件清单（≥ 3 个）

- `<ArticleCard>`：被 /、/categories/:slug、/tags/:slug、/search 共用
- `<ArticleStatusBadge>`：被 /articles/:slug 和 /admin 共用
- `<CategoryTree>`：被 /categories/:slug 和 /admin 共用
- `<CommentList>`：被 /articles/:slug 使用
- `<MarkdownEditor>`：被 /admin 使用
- `<StatsCards>`：被 / 使用
- `<TimelineList>`：被 /archives 使用

### 7. 共享 hooks/utils 清单（≥ 2 个）

- `useAuth()`：登录态与角色判断
- `useArticleStats()`：站点文章/分类/标签/字数统计
- `useArchive()`：文章按年月归档分组
- `utils/dateFormat.ts`：日期时间格式化
- `utils/markdown.ts`：Markdown 渲染与目录提取
- `utils/slug.ts`：标题转 slug 与 slug 校验

### 8. 后端中间件清单（≥ 2 个）

- `auth.go`：验证 JWT，将用户信息注入 gin.Context（`c.Set("user", ...)`）
- `rbac.go`：基于用户角色校验权限
- `rate_limiter.go`：接口限流
- `error_handler.go`：统一错误响应格式
- `logger.go`：请求日志记录
- `cors.go`：跨域配置

### 9. 技术要求

- **英文短名（用于 Docker Compose）**：`gbblog`
- **前端端口**：8101
- **后端端口**：3101
- **前端框架**：React 18 + TypeScript
- **UI 库**：Ant Design 5
- **构建工具**：Vite
- **后端框架**：Go 1.22 + Gin + GORM
- **数据库**：PostgreSQL 15 + Redis
- **ORM**：GORM
- **认证方式**：JWT + RBAC（使用 `github.com/golang-jwt/jwt/v5`）
- **其他依赖**：`github.com/gin-gonic/gin`、`gorm.io/gorm`、`gorm.io/driver/postgres`、`github.com/gin-contrib/cors`、`golang.org/x/crypto/bcrypt`、`github.com/go-playground/validator/v10`、`github.com/redis/go-redis/v9`

### 10. 文件结构强制清单

前端必须包含：
```
frontend/src/
├── api/              # user.ts, article.ts, category.ts, tag.ts, comment.ts
├── stores/           # authStore.ts, userStore.ts, articleStore.ts, categoryStore.ts, commentStore.ts
├── types/            # 共享类型定义
├── components/common/# ArticleCard, ArticleStatusBadge, CategoryTree, CommentList, MarkdownEditor, StatsCards, TimelineList
├── hooks/            # useAuth.ts, useArticleStats.ts, useArchive.ts
├── pages/            # Home, ArticleDetail, CategoryArticles, TagArticles, Archives, Search, Admin, Login
├── router/           # index.ts + guards.ts
├── utils/            # dateFormat.ts, markdown.ts, slug.ts, request.ts
└── constants/        # article.ts, comment.ts, user.ts, errorCodes.ts
```

后端必须包含（Go 标准布局）：
```
backend/
├── go.mod
├── go.sum
├── cmd/
│   └── server/
│       └── main.go              # 程序入口：加载配置、初始化 DB/Redis、装配依赖并启动 Gin
└── internal/
    ├── config/                  # config.go（数据库连接、Redis、JWT、限流、端口等）
    ├── model/                   # 按实体分文件：user.go, category.go, tag.go, article.go, comment.go, article_tag.go
    ├── repository/              # 按实体分文件：user_repository.go, category_repository.go, tag_repository.go, article_repository.go, comment_repository.go
    ├── service/                 # 按实体分文件：user_service.go, category_service.go, tag_service.go, article_service.go, comment_service.go
    ├── handler/                 # 按实体分文件：user_handler.go, category_handler.go, tag_handler.go, article_handler.go, comment_handler.go
    ├── router/                  # router.go + 按实体分文件：users.go, categories.go, tags.go, articles.go, comments.go
    ├── middleware/              # auth.go, rbac.go, rate_limiter.go, error_handler.go, logger.go, cors.go
    ├── dto/                     # 请求/响应结构体
    ├── constants/               # article.go, comment.go, user.go, error_codes.go, log_templates.go, messages.go
    └── util/                    # jwt.go, logger.go, formatters.go, app_error.go, slug.go, markdown.go, search.go
```

**严禁合并职责到单一文件。即使是简单功能，也必须按照上述分层结构拆分，确保后续每一个小改动都至少触达 3-5 个文件。**

### 11. 特殊约束：屎山代码设计（低内聚、高耦合、牵一发动全身）

为了验证模型的跨文件协同改动能力，本项目在规范分层的基础上，额外要求刻意制造合理的"屎山"耦合：

1. **日志模块必须单独管理但全栈引用**
   - 后端必须有一个 `internal/util/logger.go`，使用 `log/slog` 或 `zap` 封装
   - 所有 handler、service、middleware 都必须引用 logger
   - 日志格式字符串必须集中定义在 `internal/constants/log_templates.go`，包含至少 25 条模板，如 `ARTICLE_PUBLISH_SUCCESS`、`COMMENT_AUDIT_FAILED`
   - 任何业务字段变更（如 Article 新增字段）都必须同步修改对应的日志模板和调用处

2. **异常信息必须分散且层层透传**
   - 错误码集中在 `internal/constants/error_codes.go`，但每个 service/handler 都要手动拼接 message
   - 禁止在单一地方统一处理所有异常，必须在 handler 中再次包装 service 返回的错误
   - 错误 message 中必须包含实体名、字段名、角色名，如 `"Article[id=123] publish failed: category not match"`

3. **常量/工具类必须多处耦合**
   - `internal/util/formatters.go` 中同时包含日期格式化、字数格式化、状态文本、角色文本、评论状态文本
   - 多个 handler 和 service 都直接引用 formatters，而不是独立拆分
   - `internal/constants/messages.go` 同时包含前端提示文案、后端返回文案、日志文案
   - 修改一个文案可能同时影响前端显示、后端返回和日志内容

4. **文章状态机必须跨多处定义**
   - ArticleStatus 的流转规则在 service 状态机、前端按钮显隐、日志模板、错误码、formatters 中同时存在
   - 新增一个状态值需要修改至少 10 处

5. **枚举必须前后端重复定义且被多处引用**
   - ArticleStatus、CommentStatus 和 UserRole 必须在前端 constants、后端 constants、类型定义、日志模板、错误码、formatters、GORM 模型中同时存在
   - 新增一个枚举值时，必须修改至少 10 处文件

6. **牵一发动全身的验证标准**
   - 后续如果要求"给 Article 新增一个 'series' 系列类型"，必须触达：GORM AutoMigrate、Article 模型、前后端 constants、ArticleService、ArticleCard、CategoryTree、formatters、日志模板、错误码、首页推荐逻辑、筛选器等 ≥ 10 个文件

## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。

### 12. 部署要求

这是前后端分离项目。

前端代码放在 `frontend/`：
- 页面、组件、路由、状态管理、API 请求、类型定义、样式和资源等放在对应的子文件夹

后端代码放在 `backend/`：
- 数据库脚本放在 `database/`。

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=gbblog`
- `frontend/Dockerfile`：多阶段构建，先构建再用 Nginx 托管
- `backend/Dockerfile`：Go 多阶段构建（见下方骨架）
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明；同时包含 `COMPOSE_PROJECT_NAME` 默认值

backend/Dockerfile 使用以下 Go 多阶段构建骨架：
```
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: gbblog`
- 所有服务使用 `container_name` 命名容器，容器名带 `${COMPOSE_PROJECT_NAME:-gbblog}` 前缀
- 数据库数据使用命名卷持久化（`volumes:` 顶层声明，不要使用绑定挂载到中文路径）
- 前端通过 Nginx 反向代理后端 API（前端代码里调用 `/api`，由 Nginx 转发到 `backend:8080`，不要硬编码 `localhost`）
- 服务间通过 Docker 内部网络通信，服务名即主机名
- 数据库服务配置 `healthcheck`，后端服务通过 `depends_on: { db: { condition: service_healthy } }` 等待数据库就绪
- Redis 服务配置 `healthcheck`，后端服务同样等待 Redis 就绪
- 后端服务也配置 `healthcheck`，前端 `depends_on` 后端健康
- 暴露端口：前端 `8101:80`，后端 `${BACKEND_PORT:-3101}:8080`，数据库 `${DB_PORT:-5601}:5432`，Redis `${REDIS_PORT:-6501}:6379`
- 所有可变配置通过环境变量注入，并在 `.env.example` 中给出默认值
- 支持通过 `docker compose up -d` 一条命令启动全部服务

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME=gbblog`
- 数据库连接相关：`DB_NAME=gbblog_db`、`DB_USER=gbblog_user`、`DB_PASSWORD=gbblog_pwd`、`DB_ROOT_PASSWORD=gbblog_root`
- Redis：`REDIS_PORT=6501`
- 后端：`JWT_SECRET=change_me_to_a_long_random_string`
- 端口映射：`FRONTEND_PORT=8101`、`BACKEND_PORT=3101`、`DB_PORT=5601`

Nginx 反向代理要求（`frontend/nginx.conf`）：
- `location /api/` 反向代理到 `http://backend:8080/`
- 配置 `try_files $uri $uri/ /index.html;` 支持前端 SPA 路由

### 13. Git 要求

代码编写完成后初始化 Git + 创建 `.gitignore`，`.gitignore` 内容按项目技术栈适配：
```
node_modules/
dist/
.env
.DS_Store
*.log
.idea/
.vscode/
backend/server
backend/bin/
*.test
*.out
backend/coverage/
frontend/coverage/
```

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：
- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：
- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示，后端一栏写 `Go 1.22 + Gin + GORM`
- 后端本地开发命令为 `cd backend && go mod tidy && go run ./cmd/server`（或 `go run .`），构建命令为 `go build ./...`
- 不要保留脚手架默认 README 内容
- 必须包含"枚举出现位置清单"，列出 ArticleStatus、CommentStatus 和 UserRole 在前后端的所有出现位置

## 自检清单（生成代码后必须确认）

1. 在中文目录名下执行 `docker compose config --quiet` 不报错
2. `docker compose up -d` 后所有容器 healthy
3. 浏览器访问 `http://localhost:8101` 能看到页面，且 `/api/...` 请求返回正常
4. README 中的 Docker 启动命令是 `docker compose up -d`
5. 后端在 `backend/` 下执行 `go mod tidy` 与 `go build ./...` 不报错
6. 核心实体 ≥ 3 个，每个实体明确贯穿全栈
7. 核心页面 ≥ 4 个，至少 2 个页面共用 `<ArticleCard>` 或 `<ArticleStatusBadge>`
8. 横切关注点 ≥ 2 个，每个写明触达文件层
9. 共享枚举 ≥ 2 组，README 列出所有出现位置
10. 共享前端组件 ≥ 3 个，共享 hooks/utils ≥ 2 个
11. 后端中间件 ≥ 2 个
12. 文件结构强制清单已写入
13. 包含"严禁合并职责到单一文件"
14. 包含屎山代码设计要求
